from pythonwhat import parsing

class With(object):
  withs = []

  @staticmethod
  def find():


class WithParser(parsing.Parser):
  def __init__(self):
    self.withs = []

  def visit_With(self, node):

    for item in node.items:
      item.context_expr

with_1 = With.findSome().compare(With.findSolution(1)).
with_1.hasArgument("That isn't the name we asked for.")

"There is a problem with a `with` statement. "
